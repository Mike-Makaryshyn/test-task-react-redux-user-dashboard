
import styles from "./MaterialCompleted.module.scss";

const MaterialCompleted: React.FC = () => {
  return (
    <div className={`${styles.completed_wrapper} tile`}>
      test
    </div>
  );
};

export default MaterialCompleted;
